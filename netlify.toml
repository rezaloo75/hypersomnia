[build]
  base    = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

# VITE_API_BASE_URL must be set in Netlify dashboard → Site settings → Environment variables
# Set it to your Railway backend URL, e.g. https://hypersomnia-backend.up.railway.app
